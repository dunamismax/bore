---
import "../styles/global.css";
import { primaryRoutes } from "../lib/routes";

type Props = {
  title: string;
  description: string;
};

const { title, description } = Astro.props as Props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body>
    <div class="shell">
      <header class="site-header">
        <div class="eyebrow">Bore v2 relay transfer</div>
        <div class="title-row">
          <div>
            <h1>Bore</h1>
            <p class="lede">
              Encrypted peer-to-peer file transfer with relay fallback. The v2
              web lane now supports end-to-end encrypted file transfers.
            </p>
          </div>
          <nav aria-label="Primary">
            <ul class="nav-list">
              {
                primaryRoutes.map((route) => (
                  <li>
                    <a href={route.href}>{route.label}</a>
                  </li>
                ))
              }
            </ul>
          </nav>
        </div>
      </header>
      <main class="page-content">
        <slot />
      </main>
    </div>
  </body>
</html>
